<div id="<%= dom_id post %>">
  <div>
    <strong>Title:</strong>
    <%= post.title %>
  </div>

  <div>
    <strong>Body:</strong>
    <%= post.body %>
  </div>

<p>
  <strong>Author:</strong>
  <%= post.user.username.presence || post.user.email %>
</p>

</div>
